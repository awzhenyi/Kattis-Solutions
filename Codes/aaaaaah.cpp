#include <iostream>
#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
	string a,b;
	cin >> a >> b;
	if(a.length()<b.length())
		cout << "no";
	else
		cout << "go";
	return 0;
} 