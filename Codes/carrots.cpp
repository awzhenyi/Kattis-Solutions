#include <iostream>
#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
	int n,p;
	cin >> n >> p;
	while(n--) {
		string s;
		getline(cin, s);
	}
	cout << p;
	return 0;
}